package dev.frost.miniverse.client;

// Main-class stub renamed so it does not conflict with the real client implementation
// which lives in src/client/java. This stub remains in main sources to avoid
// client-only imports being compiled on the server/classpath, but it must not
// share the same binary name as the client entrypoint to avoid classloader
// conflicts during development/runtime.
final class MiniverseClientStub {
    // Intentionally empty.
}


