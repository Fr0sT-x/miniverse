package dev.frost.miniverse.client.gui;

// Stub left in main sources to avoid client-only imports being compiled on the server/classpath.
// Rename the stub class so it does not share the same binary name as the real
// client implementation (which lives in src/client/java). Keeping the stub
// package-private avoids requiring the filename to match the class name.
final class SessionScreenStub {
    // Real implementation lives in src/client/java as `SessionScreen`.
}



